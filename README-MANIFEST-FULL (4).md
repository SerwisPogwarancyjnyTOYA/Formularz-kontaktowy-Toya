# Pełny manifest Drive

Docelowy plik z eksportu Apps Script zapisz tutaj jako:

```text
data/drive-drawings-map.full.json
```

Strona wczytuje najpierw `drive-drawings-map.full.json`, a jeżeli go nie ma, wraca do obecnego seedowego `drive-drawings-map.json`.
